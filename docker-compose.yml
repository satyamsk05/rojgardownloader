services:
  web:
    build: .
    ports:
      - "80:8000"
    volumes:
      - ./downloads:/app/downloads
      - ./web_app/stats.json:/app/web_app/stats.json
    restart: always
    environment:
      - PORT=8000
